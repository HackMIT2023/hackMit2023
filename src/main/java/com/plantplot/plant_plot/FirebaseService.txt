package com.plantplot.plant_plot;

import com.google.firebase.database.*;
import org.springframework.stereotype.Service;

@Service
public class FirebaseService {

    private final DatabaseReference databaseReference;

    public FirebaseService() {
        databaseReference = FirebaseDatabase.getInstance().getReference("your-database-node");
    }

    public void writeToDatabase(String data) {
        DatabaseReference childReference = databaseReference.child("child-node");
        childReference.setValueAsync(data);
    }

    public String readFromDatabase() {
        // Implement your read logic here
        // Example: ValueEventListener
        return "Data from database";
    }
}
